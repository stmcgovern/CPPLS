//just in material_data.h now


// #include "material_data.h"

// //not needed now

//   // At present, material data is stored for 4 different types of
//   // material. This, as well, may easily be extended in the future.
//   MaterialData::MaterialData (const unsigned int n_groups)
//     :
//     n_groups (n_groups),
//     n_materials (8),
//     diffusion (n_materials, n_groups),
//     sigma_r (n_materials, n_groups),
//     nu_sigma_f (n_materials, n_groups),
//     sigma_s (n_materials, n_groups, n_groups),
//     chi (n_materials, n_groups)
//   {

//     const std::vector<double> heat_conductivity(n_rock_types);
//     const std::vector<double> surface_porosity(n_rock_types);
//     const std::vector<double> surface_permeability(n_rock_types);
//     const std::vector<double> solid_density(n_rock_types);
//     const std::vector<double> compressibility_coefficient(n_rock_types);
//   }
